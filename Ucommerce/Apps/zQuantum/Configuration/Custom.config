<!--
	Custom configuration for uCommerce components.
	Find the ID of component you want to override
	and use the same ID here. Your component will
	override the default.
	You can configure new components here as well
	and have them injected into existing ones.
-->
<configuration>
	<components>
		<component id="ProductsIndexDefinition"
		           service="Ucommerce.Search.IIndexDefinition`1[[Ucommerce.Search.Models.Product, Ucommerce.Search]], Ucommerce.Search"
		           type="SitefinityWebApp.Mvc.Indexes.QuantumProductIndexDefinition, SitefinityWebApp">
		</component>
	</components>
	<partial-component id="Initialize">
		<parameters>
			<tasks>
				<array>
					<value remove="${Initialize.ExecuteUpdateCurrencyExchangeRatesPipelineTask.Async}"/>
					<value remove="${Initialize.ReinitializeWebApiControllerCacheTask}"/>
				</array>
			</tasks>
		</parameters>
	</partial-component>
</configuration>